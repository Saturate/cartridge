---
"cartridge": minor
---

Add headless agent mode: spawn agents with piped stdio instead of PTY for structured output. Supported by Claude (-p --output-format json), Codex, OpenCode (run --format json), and custom commands.
